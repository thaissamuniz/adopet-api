const users = require('./User.js');
const pets = require('./Pet.js');
const adoptions = require('./Adoption.js');
const roles = require('./Role.js');

module.exports = {
    users,
    pets,
    adoptions,
    roles
}
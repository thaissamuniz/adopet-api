const UserController = require('../../controllers/UserController.js');
const user = require('../../models/User.js');

describe('testando o controller dos usuarios', () => {
    it('deve salvar usuario no db', () => {

    })
})